---

import Footer from "../components/elements/Footer.astro";
import Navbar from "../components/elements/Navbar.astro";
import {SEO} from "astro-seo";


export interface Props {
  title: string;
  value: string;
  description: string;
}

const { title } = Astro.props;
const canonicalURL = new URL(Astro.url.pathname, Astro.site).toString()

const resolvedImageWithDomain = new URL("/images/cooroora-pic.webp", Astro.site).toString()

const description="Hinterland Web offers premium web design and developement services for local businesses in Noosa Hinterland and beyond. The mission is simple: to help you establish and enhance your online presence, no matter where you're starting from! Contact André today!"
   

---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/images/cooroora-pic.webp" />
    <meta name="generator" content={Astro.generator} />
    <meta name="description" content={description} />

    <SEO
    description={description}
    canonical={canonicalURL}
    openGraph={{
      basic: {
        title: "Hinterland Web",
        type:"website",
        image: resolvedImageWithDomain,
        url: canonicalURL,
      },
    }}
    extend={{
      meta: [
        {name: "keywords", content:"web design, website designer SEO, sunshine coast web design, websites, web agency in Noosa, affordable websites in noosa, world class web design, website, websites near me, web design expert, web developer, pomona web developer, web design Pomona, website development Noosa, local business online presence, affordable web design sunshine coast, professional website services, Hinterland Web portfolio, mobile-friendly websites, custom web solutions, website testimonials, web design pricing, logo design Noosa, branding services Pomona, visual identity solutions, creative graphic design, brand development, bespoke logo creation, business branding, Hinterland Web  portfolio, graphic design testimonials"}
      ]
    }}
    />
    
    
    <meta name='impact-site-verification' content='40885628-69a1-4177-9671-d7fa6a7769f3'>
    <title>{title}</title>

    <link
      href="https://fonts.googleapis.com/css2?family=Raleway:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link rel="sitemap" href="/sitemap-index.xml" />
    
  </head>
  <body class="overflow-hidden overflow-y-auto bg-body">
    <Navbar />
    <slot />
  
    <Footer />
    
  </body>
  
  <script>
  
    function handleThemeToggle() {
        const switchTheme = document.querySelector(
          "[data-switch-theme]"
        ) as HTMLButtonElement;

        if (switchTheme) {
          const initTheme = () => {
            if (
              localStorage.getItem("appTheme") === "dark" ||
              (!("appTheme" in localStorage) &&
                window.matchMedia("(prefers-color-scheme: dark)").matches)
            ) {
              document.documentElement.classList.add("dark");
            } else {
              document.documentElement.classList.remove("dark");
            }
          };

          initTheme();

          const toggleTheme = () => {
            const doc = document.documentElement;
            if (doc) {
              if (localStorage.getItem("appTheme")) {
                if (localStorage.getItem("appTheme") === "light") {
                  doc.classList.add("dark");
                  localStorage.setItem("appTheme", "dark");
                } else {
                  doc.classList.remove("dark");
                  localStorage.setItem("appTheme", "light");
                }
              } else {
                if (doc.classList.contains("dark")) {
                  doc.classList.remove("dark");
                  localStorage.setItem("appTheme", "light");
                } else {
                  doc.classList.add("dark");
                  localStorage.setItem("appTheme", "dark");
                }
              }
            }
          };

          switchTheme.addEventListener("click", toggleTheme);
        }
      }

      // Run on initial page load
      handleThemeToggle();

   
  </script>
  </script>
</html>
<style is:global>
  :root {
    --color-bg: 216 229 207;
    --color-border: 216 229 207;
    --color-box: 216 229 207;
    --box-border: 20 49 68;
    --box-sd: 20 49 68 / 0.5;
	--heading-1:  20 49 68;
	--heading-2: 20 49 68;
	--heading-3: 20 49 68;
  }

  .dark {
    --color-bg: 20 49 68;
    --color-box: 17 24 39;
    --box-border: 243 244 246/0.1;
    --box-sd: transparent;

	
	--heading-1:  216 229 207;
	--heading-2:  216 229 207;
	--heading-3:  209 213 219;
  }

  html{
    scroll-behavior: smooth;
  }
  body {
    font-family: "Raleway", sans-serif;
  }

  img {
        content-visibility: auto;
      }

  [data-toggle-nav][data-open-nav="true"] #line1 {
    transform: translateY(0.375rem) rotate(40deg);
  }
  [data-toggle-nav][data-open-nav="true"] #line2 {
    transform: scaleX(0);
    opacity: 0;
  }
  [data-toggle-nav][data-open-nav="true"] #line3 {
    transform: translateY(-0.375rem) rotate(-40deg);
  }

  
  [data-nav-overlay][data-is-visible="true"] {
    visibility: visible;
    display: flex;
  }
</style>
