---
import Service from "../cards/Service.astro";
import Container from "../shared/Container.astro";
import Paragraph from "../shared/Paragraph.astro";
import Title from "../shared/Title.astro";
import { services } from "../../utils/data";
import Subtitle from "components/shared/Subtitle.astro";


type ServicesProps = {
    title: string;
    description: string;
    icon: string;
    url?:string;
};


---

<section id="services">
    <Container className="space-y-10 md:sapce-y-12">
        <div class="text-center max-w-3xl mx-auto space-y-4">
            <h2 class="font-bold text-5xl text-secondary dark:text-tertiary">
                What we offer
            </h2>
            <Paragraph>
                Be it simple or complex, our focus is on effective solutions that help <br><strong>you and your business to grow</strong>.
            </Paragraph>
        </div>
        
        <div class="grid sm:grid-cols-2 lg:grid-cols-2 gap-6 lg:gap-8">
            {
                    services.map((service: ServicesProps) => {
                    return (
                            <Service title={service.title} description={service.description} icon={service.icon} url={service.url}>
                            </Service>
                            );
                    })
            }
        </div>
    </Container>
</section>